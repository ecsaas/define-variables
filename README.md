# define-variables

```shell
 git init
 git branch -m main

 git config user.name manhbv.giaminh
 git config user.email manhbv.giaminh@gmail.com
 git add .
 git commit -m 'define-variables'

 git remote add origin git@github.com:ecsaas/define-variables.git
 git push origin main
```
